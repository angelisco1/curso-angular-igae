import { ToEmojiActivoPipe } from './to-emoji-activo-pipe';

describe('ToEmojiActivoPipe', () => {
  it('create an instance', () => {
    const pipe = new ToEmojiActivoPipe();
    expect(pipe).toBeTruthy();
  });
});
